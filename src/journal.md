# Журнал работ

## 26.06.2026 — Этап 1. Каркас проекта
**Что сделано:**
- Создан форк репозитория на GitHub
- Проект склонирован локально
- Создана структура папок (`src/`)
- Заполнен `student_meta.md` (группа, ФИО, вариант «полный»)
- Написан `main.py` с парсингом аргументов командной строки
- Создана база данных `index.db` с таблицей `files`

**Проблемы:**
- Python изначально не был добавлен в PATH - пришлось переустановить и поставить галочку "Add Python to PATH"
- Git не был установлен - скачали и установили

**Следующий шаг:** Реализация сканирования файлов

---

## 27.06.2026 — Этап 2. Сканирование (ручная рекурсия)
**Что сделано:**
- Создан `scanner.py` с ручным рекурсивным обходом папок (через `pathlib.Path.iterdir()`)
- Реализован сбор данных: путь, размер, дата изменения
- Данные сохраняются в SQLite, обновляются при повторном запуске
- Добавлена история сканирований в таблицу `scans`

**Проблемы:**
- Первая версия использовала `os.walk` - это запрещено в полном варианте → переделано на ручную рекурсию
- Ошибка с путями при запуске из разных директорий - исправлено через `Path` и абсолютные пути

**Следующий шаг:** Добавление фильтра и хеширования

---

## 27.06.2026 — Этап 2. Фильтр по расширению
**Что сделано:**
- Добавлен аргумент `--extensions` в `main.py`
- В `scanner.py` реализована фильтрация файлов по расширению
- Проверено: `--extensions .py .md` — сканируются только указанные типы

**Проблемы:** нет

**Следующий шаг:** Реализация поиска дубликатов

---

## 28.06.2026 — Этап 3. Дубликаты
**Что сделано:**
- В `scanner.py` добавлено вычисление SHA-256 хеша для каждого файла
- Создан `duplicates.py` для поиска групп файлов с одинаковым хешем
- Результат выводится в консоль с указанием количества копий
- Хеши сохраняются в SQLite для повторного использования

**Проблемы:**
- При сохранении файлов в БД возникала ошибка с колонкой `indexed_at` — старая база данных не имела этой колонки → удалил `index.db` и пересоздал заново

**Следующий шаг:** Реализация сравнения с резервной копией

---

## 28.06.2026 — Этап 4. Резервная копия (начало)
**Что сделано:**
- Создан `backup.py` для сравнения двух папок
- Реализован сбор информации о файлах бэкапа (MD5-хеши)
- Добавлен аргумент `--backup` в `main.py`

**Проблемы:**
- Ошибка импорта модулей в PyCharm — Python не находил `database` и другие модули → добавлен `sys.path.append` в `main.py`
- Файлы сохранились в неправильной кодировке (UTF-8 с BOM) - пересозданы через PyCharm

**Следующий шаг:** Доработка вывода и сохранения в БД

---

## 29.06.2026 — Этап 4. Резервная копия (завершение)
**Что сделано:**
- Доработан вывод: три категории (отсутствующие, изменённые, лишние)
- Добавлено сохранение результатов в таблицу `backup_checks` в SQLite
- Проведено тестирование на реальных данных

**Проблемы:**
- Команда `sqlite3` не распознавалась в PowerShell - проверил через Python-скрипт, данные сохранились корректно

**Следующий шаг:** Итоговое тестирование всех этапов

---

## 29.06.2026 — Итоговое тестирование
**Что сделано:**
- Проиндексирована папка `src` → 14 файлов, 29 КБ
- Проверен фильтр `--extensions .py .md` - отфильтрованы нужные файлы
- Запущен поиск дубликатов → дубликатов не найдено
- Выполнено сравнение с тестовой резервной копией - отчет корректен
- Проверено сохранение в БД (запись в `backup_checks`)
- Все этапы работают без ошибок

**Проблемы:** нет

**Следующий шаг:** Завершение проекта

---

## 29.06.2026 — Завершение проекта
**Что сделано:**
- Все модули готовы и протестированы
- Журнал работ заполнен
- Код отправлен на GitHub
- Проект соответствует полному варианту практики

**Итог:** Полный индексатор с SQLite, ручной рекурсией, фильтром, дубликатами и проверкой резервной копии готов к сдаче.
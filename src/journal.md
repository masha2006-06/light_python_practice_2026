# Журнал работ

## 26.06.2026 — Этап 1. Каркас проекта

**Что сделано:**
- Сделала форк репозитория на GitHub.
- Склонировала проект на компьютер.
- Создала папку `src/` для кода.
- Заполнила `student_meta.md` (группа, ФИО, вариант «полный»).
- Написала `main.py` с обработкой аргументов командной строки.
- Настроила базу данных `index.db` с таблицей `files`.

**Проблемы:**
- Python не был прописан в PATH, пришлось переустанавливать и ставить галочку "Add Python to PATH".
- Git тоже не был установлен, скачала и настроила.

**Следующий шаг:** Начать писать сканирование файлов.

---

## 27.06.2026 — Этап 2. Сканирование (ручная рекурсия)

**Что сделано:**
- Реализовала `scanner.py` с ручным обходом папок через `pathlib.Path.iterdir()`.
- Собираю путь, размер, дату изменения.
- Данные сохраняются в SQLite, обновляются при повторном запуске.
- Добавила таблицу `scans` для истории.

**Проблемы:**
- Сначала использовала `os.walk`, но это запрещено в полном варианте — переделала на ручную рекурсию.
- Была ошибка с путями, когда запускала из разных мест — исправила через `Path`.

**Следующий шаг:** Добавить фильтр по расширениям и начать хеширование.

---

## 27.06.2026 — Этап 2. Фильтр по расширению

**Что сделано:**
- Добавила аргумент `--extensions` в `main.py`.
- В `scanner.py` теперь можно отфильтровать файлы по расширению.
- Проверила на `--extensions .py .md` — работает.

**Проблемы:** нет.

**Следующий шаг:** Реализовать поиск дубликатов.

---

## 28.06.2026 — Этап 3. Дубликаты

**Что сделано:**
- В `scanner.py` добавила вычисление SHA-256 хеша для каждого файла.
- Написала `duplicates.py`, который ищет группы файлов с одинаковым хешем.
- Результат выводится в консоль, хеши сохраняются в SQLite.

**Проблемы:**
- Старая база данных не имела колонки `indexed_at`, из-за чего сыпались ошибки. Решила удалить `index.db` и пересоздать заново.

**Следующий шаг:** Приступить к сравнению с резервной копией.

---

## 28.06.2026 — Этап 4. Резервная копия (начало)

**Что сделано:**
- Создала `backup.py` для сравнения двух папок.
- Реализовала сбор информации о файлах бэкапа через MD5.
- Добавила аргумент `--backup` в `main.py`.

**Проблемы:**
- В PyCharm были проблемы с импортами — Python не видел модули. Добавила `sys.path.append` в `main.py`.
- Файлы сохранились в неправильной кодировке, пересоздала через PyCharm.

**Следующий шаг:** Доделать вывод и сохранение в БД.

---

## 29.06.2026 — Этап 4. Резервная копия (завершение)

**Что сделано:**
- Доделала вывод: теперь показывает три категории — отсутствующие, изменённые и лишние файлы.
- Результаты сохраняются в таблицу `backup_checks` в SQLite.
- Проверила на реальных данных — всё работает.

**Проблемы:**
- В PowerShell не работала команда `sqlite3`, но через Python-скрипт проверила — данные сохраняются.

**Следующий шаг:** Полное тестирование всех этапов.

---

## 29.06.2026 — Итоговое тестирование

**Что сделано:**
- Проиндексировала папку `src` — 14 файлов, 32.4 КБ.
- Проверила фильтр `--extensions .py .md` — отфильтровались нужные файлы.
- Запустила поиск дубликатов — дубликатов нет.
- Сравнила с тестовой резервной копией:
  - 12 файлов отсутствуют в бэкапе
  - 1 файл изменён (`main.py`)
  - 1 лишний файл в бэкапе (`extra.txt`)
- Проверила, что запись сохранилась в БД.

**Проблемы:** нет.

**Следующий шаг:** Оформить журнал и сдать проект.

---

## 29.06.2026 — Завершение проекта

**Что сделано:**
- Все модули готовы и протестированы.
- Журнал работ заполнен.
- Коммиты разбиты по этапам:
  - `a2c45ab` — каркас и БД
  - `b5b1736` — сканирование
  - `df52b71` — дубликаты
  - `9158e98` — резервная копия
  - `3a2270d` — журнал
  - `1c0f5d6` — .gitignore
- Всё загружено на GitHub.

**Итог:** Полный вариант индексатора с SQLite, ручной рекурсией, фильтром, дубликатами и проверкой бэкапа — готов.

---

## 01.07.2026 — Доработка проекта
**Что сделано:**
- Создана папка `data/` для базы данных
- Путь к БД изменён на `data/index.db`
- Удалены временные файлы: `__pycache__/`, `backup/`, `.idea/`
- Добавлен `test.py` для автоматической проверки
- Обновлён `.gitignore`
- Добавлен `requirements.txt` с пояснением

**Проблемы:** нет
**Следующий шаг:** Проект готов к сдаче